// Keep this value synchronized with the newest checked-in migration. Runtime
// code cannot enumerate migration files inside a Cloudflare Worker bundle.
export const EXPECTED_SCHEMA_VERSION = '0009';
